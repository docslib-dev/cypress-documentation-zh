---
title: 'first | Cypress 文档'
description: 在Cypress中获取一组DOM元素中的第一个DOM元素。
sidebar_label: first
slug: /api/commands/first
---

<ProductHeading product="app" />

# first

获取一组DOM元素中的第一个DOM元素。

:::info

该命令的查询行为与jQuery中的
[`.first()`](http://api.jquery.com/first)
完全一致。

:::

## 语法

```javascript
.first()
.first(options)
```

### 用法

<Icon name="check-circle" color="green" /> **正确用法**

```javascript
cy.get('nav a').first() // 获取导航中的第一个链接
```

<Icon name="exclamation-triangle" color="red" /> **错误用法**

```javascript
cy.first() // 错误，不能直接链式调用'cy'
cy.getCookies().first() // 错误，'getCookies'不返回DOM元素
```

### 参数

<Icon name="angle-right" /> **options _(Object)_**

传入一个选项对象以改变`.first()`的默认行为。

| 选项      | 默认值                                                           | 描述                                                                         |
| --------- | ----------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `log`     | `true`                                                            | 在[命令日志](/app/core-concepts/open-mode#Command-Log)中显示该命令 |
| `timeout` | [`defaultCommandTimeout`](/app/references/configuration#Timeouts) | 在[超时](#Timeouts)前等待`.first()`解析的时间               |

<HeaderYields />

- `.first()`返回它找到的新DOM元素。
- `.first()`是一个查询命令，可以安全地链式调用其他命令。

## 示例

### 无参数

#### 获取列表中的第一个列表项

```html
<ul>
  <li class="one">大拇指上的小玩意儿</li>
  <li class="two">鞋子上的小玩意儿</li>
  <li class="three">膝盖上的小玩意儿</li>
  <li class="four">门上的小玩意儿</li>
</ul>
```

```javascript
// 返回 <li class="one">大拇指上的小玩意儿</li>
cy.get('li').first()
```

## 规则

<HeaderRequirements />

- `.first()`必须链式调用在返回DOM元素的命令之后。

<HeaderAssertions />

- `.first()`会自动[重试](/app/core-concepts/retry-ability)
  直到元素
  [存在于DOM中](/app/core-concepts/introduction-to-cypress#Implicit-Assertions)。
- `.first()`会自动[重试](/app/core-concepts/retry-ability)
  直到所有链式断言通过。

<HeaderTimeouts />

- `.first()`可能会因等待元素
  [存在于DOM中](/app/core-concepts/introduction-to-cypress#Implicit-Assertions)
  而超时。
- `.first()`可能会因等待添加的断言通过而超时。

## 命令日志

**_在表单中查找第一个输入框_**

```javascript
cy.get('form').find('input').first()
```

以上命令将在命令日志中显示为：

<DocsImage
  src="/img/api/first/get-the-first-in-list-of-elements.png"
  alt="命令日志 first"
/>

当点击命令日志中的`first`时，控制台会输出以下内容：

<DocsImage
  src="/img/api/first/console-log-the-first-element.png"
  alt="console.log first"
/>

## 另请参阅

- [`.eq()`](/api/commands/eq)
- [`.last()`](/api/commands/last)